let arg,add,search,deleteval,suggest,display;_bf2‍.x([["cli",()=>cli]]);_bf2‍.w("arg",[["default",["arg"],function(v){arg=v}]]);_bf2‍.w("./main",[["add",["add"],function(v){add=v}],["search",["search"],function(v){search=v}],["deleteval",["deleteval"],function(v){deleteval=v}],["suggest",["suggest"],function(v){suggest=v}],["display",["display"],function(v){display=v}]]);//https://www.twilio.com/blog/how-to-build-a-cli-with-node-js
//https://www.youtube.com/watch?v=AXjmTQ8LEoI



function parseArgumentsIntoOptions(rawArgs) {
  const args = arg(
    {
      '--add': Boolean,
      '--delete': Boolean,
      '--search': Boolean,
      '--suggest': Boolean,
      '--display': Boolean,
    },
    {
      argv: rawArgs.slice(2),
    }
  );
  return {
    var: args._[0],
    add: args['--add'] || false,
    delete: args['--delete'] || false,
    search: args['--search'] || false,
    suggest: args['--suggest'] || false,
    display: args['--display'] || false,
  };
}

/*
https://stackoverflow.com/questions/19132867/adding-firebase-data-dots-and-forward-slashes
*/
       function cli(args) {
  let options = parseArgumentsIntoOptions(args);
  if (options['add']) {
    //console.log(options['var']);
    add(options['var']);
  }
  else if (options['delete']) {
    deleteval(options['var']);
  }
  else if (options['search']) {
    search(options['var']);
  }
  else if (options['suggest']) {
    suggest(options['var']);
  }
  else if (options['display']) {
    display();
  }
}